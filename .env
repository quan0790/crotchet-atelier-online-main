# -----------------------------
# Project: Crochet Atelier
# Description: Environment variables for frontend and Netlify deployment
# -----------------------------

# API / backend URL (replace with your real API endpoint if you have one)
VITE_API_URL=https://crotchet-atelier-api.onrender.com

# Default currency for product prices
VITE_CURRENCY=KES

# Feature flags
VITE_ENABLE_DISCOUNTS=true
VITE_ENABLE_BESTSELLERS=true

# Netlify deployment variables (optional)
NETLIFY_SITE_ID=your-netlify-site-id
NETLIFY_AUTH_TOKEN=your-netlify-auth-token

# Optional public keys (replace with your actual keys if needed)
VITE_PUBLIC_KEY=your-public-key-here

# -----------------------------
# Notes:
# - All variables prefixed with VITE_ are accessible in frontend code.
# - Never commit sensitive private keys.
# - For Netlify, you can also define these environment variables in the Netlify dashboard.
# -----------------------------
